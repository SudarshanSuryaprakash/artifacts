name: Check Artifact

on: 
  [push]
  
jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
    - name: upload md artifact
      uses: actions/upload-artifact@v2
      with:
        name: mdArtifact
        path: temp/temp.txt
